

const { map, myCallbacks } = requiere('../test-map');

describe('map', () => {
    test('Debería map ser una función', () => {
        expect(typeof map).toEqual('function');
    });
    test('Debería return un array si se le pasa un array', () =>{
        expect(map[], [] myCallbacks) instanceof
    })
})